{
  "name": "@vcad/core",
  "version": "0.8.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc && cp ../../CHANGELOG.json dist/",
    "test": "vitest run"
  },
  "dependencies": {
    "@vcad/engine": "^0.1.0",
    "@vcad/ir": "^0.1.0",
    "zustand": "^5.0.0"
  },
  "devDependencies": {
    "typescript": "^5.7",
    "vitest": "^3.0"
  }
}
